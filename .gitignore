node_modules/
data/
data-*/
.env

# runtime / test artifacts
test-logs/
*.bak

# generated docs (handed over separately, not via git)
docs/*.pdf

# OS cruft
.DS_Store
CLAUDE.md

# Claude Code local config (hooks, settings — not for the public repo)
.claude/
